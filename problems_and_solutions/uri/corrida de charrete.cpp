#include <iostream>

using namespace std;

int main(){
	float a, b, c,
		d, e, f;
		
		
		cin >> a >> b >> c;
		cin >> d >> e >> f;
		
		if((b/c) > (e/f)){
			cout << d << endl;
		}else{
			cout << a << endl;
		}
		
	
	return 0;
}